var timeBlock = $("#time-block");

var saveButton = $(".save-button");

var inputText = $(".input-group");

var lightMode 

var darkMode

// var currentDay = moment();
// $("#current-day").text(currentDay.format("MMM Do, YYYY"));

var today = moment();
$("#current-day").text(today.format("dddd, MMMM Do YYYY, h:mm:ss a"));

console.log("today");

//event handler for save button - connects to textinput so that it can be saved to local storage

//moment so that jumot recognizes local time

//moment that changes colors in the body of 

// saveButton.click(event) {
//     inputText.text()
//     //input to local storage
// };

